- link:
    ~/.vimrc: vimrc
    ~/.bashrc: bashrc
    ~/.gdbinit: gdbinit
    ~/.xinitrc: xinitrc
    ~/.gitconfig: gitconfig
    ~/.bash_profile: bash_profile
    ~/.Xdefaults: Xdefaults
    ~/.vim: vim/
    ~/.emacs: emacs
    ~/.i3: i3/
    ~/.inputrc: inputrc
    ~/war: war/
- shell:
  - [mkdir -p $HOME/.vim/tmp
